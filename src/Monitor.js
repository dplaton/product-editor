import React from 'react';
import styled from 'styled-components';

const StyledMonitor = styled.div`
    border: 1px solid lightgreen;
`;

const Monitor = props => {
    return <StyledMonitor />;
};

export default Monitor;
